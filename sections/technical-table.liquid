{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  .section-{{ section.id }}-padding h5 {
    margin-bottom: 2rem;
  }

  .section-{{ section.id }}-padding .content-divider {
    border-top: 1px solid rgba(var(--color-foreground), 0.1);
    margin: 2rem 0;
  }

  .section-{{ section.id }}-padding .section-headline {
    font-size: 1.5rem;
    margin-bottom: 1.5rem;
  }

  .section-{{ section.id }}-padding .section-subheadline {
    font-size: 1.2rem;
    margin-bottom: 1rem;
  }

  .section-{{ section.id }}-padding .content-group {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem;
    margin-bottom: 2rem;
  }

  .section-{{ section.id }}-padding .content-item {
    font-size: 1rem; /* Consistent font size */
    line-height: 1.6; /* Consistent line-height */
  }

  .section-{{ section.id }}-padding .content-item strong {
    display: block;
    font-weight: bold;
    margin-bottom: 0.5rem;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
{%- endstyle -%}

<div class='color-{{ section.settings.color_scheme }} gradient'>
  <div class='page-width isolate scroll-trigger animate--slide-in section-{{ section.id }}-padding twcss-grid twcss-gap-32'>
    {%- if section.settings.heading != blank -%}
      <h2 class='title title-wrapper--no-top-margin inline-richtext {{ section.settings.heading_size }}{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}'>
        {{ section.settings.heading }}
      </h2>
    {%- else -%}
      <h2 class='visually-hidden'>{{ 'templates.contact.form.title' | t }}</h2>
    {%- endif -%}

    {%- for block in section.blocks -%}
      {%- case block.type -%}
        {%- when 'divider' -%}
          <hr class="content-divider" />

        {%- when 'section_headline' -%}
          <h3 class="section-headline">{{ block.settings.headline }}</h3>

        {%- when 'section_subheadline' -%}
          <h4 class="section-subheadline">{{ block.settings.subheadline }}</h4>

        {%- when 'content' -%}
          <div class="content-group">
            <div class="content-item">
              <strong>{{ block.settings.name1 }}</strong>
              <div>{{ block.settings.description1 }}</div>
            </div>
            <div class="content-item">
              <strong>{{ block.settings.name2 }}</strong>
              <div>{{ block.settings.description2 }}</div>
            </div>
            <div class="content-item">
              <strong>{{ block.settings.name3 }}</strong>
              <div>{{ block.settings.description3 }}</div>
            </div>
            <div class="content-item">
              <strong>{{ block.settings.name4 }}</strong>
              <div>{{ block.settings.description4 }}</div>
            </div>
          </div>
      {%- endcase -%}
    {%- endfor -%}
  </div>
</div>


{% schema %}
{
  "name": "Flexible Content Section",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "inline_richtext",
      "id": "heading",
      "default": "t:sections.contact-form.settings.title.default",
      "label": "t:sections.contact-form.settings.title.label"
    },
    {
      "type": "select",
      "id": "heading_size",
      "options": [
        {
          "value": "h2",
          "label": "t:sections.all.heading_size.options__1.label"
        },
        {
          "value": "h1",
          "label": "t:sections.all.heading_size.options__2.label"
        },
        {
          "value": "h0",
          "label": "t:sections.all.heading_size.options__3.label"
        },
        {
          "value": "hxl",
          "label": "t:sections.all.heading_size.options__4.label"
        },
        {
          "value": "hxxl",
          "label": "t:sections.all.heading_size.options__5.label"
        }
      ],
      "default": "h1",
      "label": "t:sections.all.heading_size.label"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.all.colors.label",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "t:sections.all.padding.section_padding_heading"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 400,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_top",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 400,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_bottom",
      "default": 36
    }
  ],
  "blocks": [
    {
      "type": "divider",
      "name": "Trennlinie",
      "settings": []
    },
    {
      "type": "section_headline",
      "name": "Section Headline",
      "settings": [
        {
          "type": "text",
          "id": "headline",
          "label": "Überschrift",
        }
      ]
    },
    {
      "type": "section_subheadline",
      "name": "Section Subheadline",
      "settings": [
        {
          "type": "text",
          "id": "subheadline",
          "label": "Subheadline",
        }
      ]
    },
    {
      "type": "content",
      "name": "Content",
      "settings": [
        {
          "type": "text",
          "id": "name1",
          "label": "Name 1",
        },
        {
          "type": "richtext",
          "id": "description1",
          "label": "Beschreibung 1",
        },
        {
          "type": "text",
          "id": "name2",
          "label": "Name 2",
        },
        {
          "type": "richtext",
          "id": "description2",
          "label": "Beschreibung 2",
        },
        {
          "type": "text",
          "id": "name3",
          "label": "Name 3",
        },
        {
          "type": "richtext",
          "id": "description3",
          "label": "Beschreibung 3",
        },
        {
          "type": "text",
          "id": "name4",
          "label": "Name 4",
        },
        {
          "type": "richtext",
          "id": "description4",
          "label": "Beschreibung 4",
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Flexible Content Section"
    }
  ]
}
{% endschema %}
